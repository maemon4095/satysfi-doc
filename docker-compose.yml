version: '3.8'
services:
  site:
    image: node:20-slim
    restart: 'no'
    working_dir: '/site'
    tty: true
    volumes:
      - ./:/satysfi-doc:cached
      - type: volume
        source: site_node_modules
        target: /satysfi-doc/site/node_modules

volumes:
  site_node_modules:
