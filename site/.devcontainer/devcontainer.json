{
    "name": "satysfi-doc/site",
    "dockerComposeFile": "../../docker-compose.yml",
    "service": "site",
    "workspaceFolder": "/satysfi-doc",
    "forwardPorts": [
        8080
    ],
    "postCreateCommand": "apt update -y && apt upgrade -y && apt install git -y"
}